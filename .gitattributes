# Set default behavior to automatically normalize line endings
* text=auto

# Explicitly declare text files you want normalized
*.js text
*.json text

# Declare files that should be left untouched
*.bin binary